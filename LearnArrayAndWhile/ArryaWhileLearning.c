#include <stdio.h>
#include <stdlib.h>
#define _CRT_SECURE_NO_WARNINGS

void main ()
{
int array = 10;
int sum;
for (int i = 0; i < array; i++)
{
   printf("Enter Num: ");
   scanf("%d", array[i]);
}
sum = array[i] + array[i +1];
printf("the sum is: %d", sum);







}